<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SmartCampusCanteen">

        <!-- ===== LAUNCHER = LOGIN SCREEN ===== -->
        <!-- App open hone par pehle LoginActivity khulegi -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="Campus Canteen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- ===== MAIN ACTIVITY — LAUNCHER HATA DIYA ===== -->
        <!-- Pehle yahan bhi LAUNCHER tha — do launchers the isliye conflict tha -->
        <!-- Ab sirf LoginActivity launcher hai -->
        <activity
            android:name=".MainActivity"
            android:exported="false" />

        <!-- ===== BAAKI ACTIVITIES — same as before ===== -->
        <activity
            android:name=".StudentActivity"
            android:exported="false"
            android:label="Campus Canteen (Menu)" />

        <activity
            android:name=".OrderTrackingActivity"
            android:exported="false"
            android:label="My Token &amp; Status"
            android:parentActivityName=".StudentActivity" />

        <activity
            android:name=".KitchenActivity"
            android:exported="false"
            android:label="Kitchen Control Panel" />

        <activity
            android:name=".CounterActivity"
            android:exported="false"
            android:label="Counter Manager" />

    </application>

</manifest>