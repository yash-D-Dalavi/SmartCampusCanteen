<?xml version="1.0" encoding="utf-8"?>

<!-- Ye Kitchen Staff ka screen hai - Dark theme with big buttons -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/kitchen_bg">

    <!-- Header Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="70dp"
        android:background="@color/kitchen_accent"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="16dp"
        android:paddingEnd="16dp">

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="🍳 Kitchen Control Panel"
            android:textColor="#FFFFFF"
            android:textSize="20sp"
            android:textStyle="bold"/>

        <!-- Live Orders count badge -->
        <TextView
            android:id="@+id/tv_orders_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Orders: 0"
            android:textColor="#FFD700"
            android:textSize="14sp"
            android:background="@drawable/badge_bg"
            android:paddingStart="12dp"
            android:paddingEnd="12dp"
            android:paddingTop="6dp"
            android:paddingBottom="6dp"/>
    </LinearLayout>

    <!-- Section Title -->
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="TAP SWITCH TO UPDATE AVAILABILITY"
        android:textColor="#AAAAAA"
        android:textSize="12sp"
        android:textStyle="bold"
        android:letterSpacing="0.1"
        android:paddingStart="16dp"
        android:paddingTop="16dp"
        android:paddingBottom="8dp"/>

    <!-- Tab Bar — Menu Toggle | Incoming Orders -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:background="#FFFFFF"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/tab_menu"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:text="Menu Items"
            android:textColor="#FF6B35"
            android:textSize="14sp"
            android:textStyle="bold"
            android:background="@drawable/tab_active_indicator"/>

        <TextView
            android:id="@+id/tab_orders"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:gravity="center"
            android:text="Incoming Orders"
            android:textColor="#999999"
            android:textSize="14sp"/>
    </LinearLayout>

    <!-- Menu Items RecyclerView (Default Visible) -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_kitchen_menu"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:clipToPadding="false"/>

    <!-- Orders RecyclerView — pehle GONE -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rv_incoming_orders"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:visibility="gone"
        android:paddingStart="12dp"
        android:paddingEnd="12dp"
        android:clipToPadding="false"/>

</LinearLayout>